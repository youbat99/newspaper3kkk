services:
  - type: web
    name: newspaper-extractor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn extractor:app --host 0.0.0.0 --port 8000
    plan: free
